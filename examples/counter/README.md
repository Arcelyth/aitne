# Example/Counter

To run this example, you can run `make exp_counter` to generate a simple HTML file then run `make run` to run a local http server on port `8000` and then you can check the effect on your browser.
