# Example/Dyn

To run this example, you can run `make exp_dyn` to generate a simple HTML file then run `make run` to run a local http server on port `8000` and then you can check the effect on your browser.
