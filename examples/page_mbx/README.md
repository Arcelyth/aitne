# Example/Page_mbx

To run this example, you can run `make exp_page2` to generate a simple HTML file then run `make run` to run a python's http server on port `8000` and then you can check the effect on your browser.
