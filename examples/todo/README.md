# Example/TODO

To run this example, you can run `make exp_todo` to generate a simple HTML file then run `make run` to run a python's http server on port `8000` and then you can check the effect on your browser.
